<Window x:Class="LogisticSystem.View.AddEditWarehouseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogisticSystem"
        mc:Ignorable="d"
        Title="Склад" Height="180" Width="400" WindowStartupLocation="CenterOwner" Background="#E6E6FA">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Название склада:" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="tbName" Grid.Row="0" Grid.Column="1" Margin="5" Height="30"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Адрес:" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="tbLocation" Grid.Row="1" Grid.Column="1" Margin="5" Height="30"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="Сохранить" Width="80" Margin="5" Click="Save_Click" IsDefault="True"  Background="#4B0082" Foreground="White"/>
            <Button Content="Отмена" Width="80" Margin="5" IsCancel="True"  Background="#4B0082" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
