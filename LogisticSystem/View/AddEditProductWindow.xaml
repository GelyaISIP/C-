<Window x:Class="LogisticSystem.View.AddEditProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogisticSystem"
        mc:Ignorable="d"
       Title="Товар" Height="450" Width="300" WindowStartupLocation="CenterOwner" Background="#E6E6FA">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Название товара:" Margin="5"/>
        <TextBox x:Name="tbName" Grid.Row="0" Margin="105,5,5,5"/>

        <TextBlock Grid.Row="1" Text="Артикул:" Margin="5"/>
        <TextBox x:Name="tbSKU" Grid.Row="1" Margin="105,5,5,5"/>

        <TextBlock Grid.Row="2" Text="Количество по складам:" Margin="5" FontWeight="Bold"/>
        <DataGrid x:Name="dgStocks" Grid.Row="2" Margin="5" AutoGenerateColumns="False" CanUserAddRows="False" 
                  ItemsSource="{Binding StockItems, RelativeSource={RelativeSource AncestorType=Window}}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Склад" Binding="{Binding WarehouseName}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="Сохранить" Width="80" Margin="5" Click="Save_Click" Background="#4B0082" Foreground="White"/>
            <Button Content="Отмена" Width="80" Margin="5" IsCancel="True" Background="#4B0082" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
